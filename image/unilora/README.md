# unilora

UniLoRA modules (`model.py`, `layer.py`, `_buffer_dict.py`) used by the image training and reconstruction scripts in the parent directory.

No standalone entrypoint; run **`train_multi.sh`** / **`reconstruct_multi.sh`** from [`..`](../README.md).
