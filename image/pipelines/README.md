# pipelines

Python package with the Qwen-Image Diffusers pipeline implementation (`pipeline_qwenimage.py`). Imported by `train_lora_multi.py` and `reconstruct_lora_multi.py` in the parent directory.

No separate install step; use the parent [`../README.md`](../README.md) environment and run the top-level scripts.
