# descriptions

Text files used as **`DESCRIPTION_FILE`** in `train_multi.sh` and `reconstruct_multi.sh` (default: `example_descriptions.txt`).

Edit or add files here, then point `DESCRIPTION_FILE` at your path when you run training or reconstruction.
