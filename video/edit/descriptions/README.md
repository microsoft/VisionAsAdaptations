# descriptions

Text descriptions bundled per UVG-style sequence name. Some workflows use captions passed on the command line instead; see `edit_caption.sh` / `edit_merge.sh` and their `--caption` usage.
